{
  "name": "netlify-500-error-server",
  "version": "1.0.0",
  "description": "A Netlify Functions project that returns an HTTP error based on query parameter on /error",
  "scripts": {
    "dev": "netlify dev",
    "start": "netlify dev"
  },
  "devDependencies": {
    "netlify-cli": "^12.0.0"
  }
}
